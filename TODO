#TODO

- Make a keygen. Something that automatically generate keys appropriate
for the TDE, and outputs them in a file MD5-style (not a bunch of floats
but some nice printable encoded characters).
- Make it faster.